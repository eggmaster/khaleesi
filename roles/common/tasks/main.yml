---
- name: Remove yum-rhn-plugin
  command: rpm -e yum-rhn-plugin rhn-check --nodeps
  ignore_errors: yes

- name: Ensure libselinux-python
  command: rpm -q libselinux-python

- name: Yum clean all
  command: yum clean all

